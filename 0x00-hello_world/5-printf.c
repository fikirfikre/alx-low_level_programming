#include <stdio.h>

/**
 * main - it will print "with proper grammer, but the outcome is a
 *                         piece of art, followed by a news line.
 *
 * Return: 0.
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
